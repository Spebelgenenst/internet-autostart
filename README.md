# internet autostart
A python script which runs a something, as soon as the device has an internet connection

## installation

```
curl -sSL https://raw.githubusercontent.com/Spebelgenenst/internet-autostart/refs/heads/main/install.sh | bash
```
if it doesn't work
```
pip3 install requests
```
